#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string
 *
 * Return: string length.
 */

int _strlen(char *s)
{
	int len;

	for (len = 0; *s != '\0'; ++len)
		return (len);
}
