#include<stdio.h>
#include<stdlib.h>
#include<time.h>

/**
 * main - Entry point
 *
 * Return: When successful 0
 */

int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
		putchar (digit);
	putchar('\n');

	return (0);
}
