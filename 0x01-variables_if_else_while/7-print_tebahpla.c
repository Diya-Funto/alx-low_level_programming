#include<stdio.h>
#include<stdlib.h>
/**
 * main - Entry point
 *
 * Return: When successful 0
 */

int main(void)
{
	char lowrev;

	for (lowrev = 'z'; lowrev >= 'z'; lowrev--)
		putchar (lowrev);
	putchar ('\n');

	return (0);
}

